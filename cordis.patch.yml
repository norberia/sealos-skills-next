# Sealos dsh profile bundle: registers the packaged use-sealos skill on
# ctx.skills. Applied after @deepseek-ai/dsh-base, which already mounts the
# skill registry and the model-facing skill loader.
- insert:
    - id: sealos-skill
      name: dsh-plugin-sealos
